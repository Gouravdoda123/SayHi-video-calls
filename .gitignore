# Root .gitignore (for backend and general project stuff)

# Node modules (root and backend)
node_modules
backend/node_modules

# Logs
*.log
npm-debug.log*

# Environment files
.env
backend/.env
frontend/.env

# System files
.DS_Store

# Optional: ignore local build/dist folders (if accidentally created in root)
dist
